name: bestIp

on:
  push:
  schedule:
    - cron: '0 */1 * * *'   # 每小时一次
  workflow_dispatch:

concurrency:
  group: bestIp-singleton
  cancel-in-progress: false

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      # ========== 关键修复：深克隆，确保远程跟踪信息准确 ==========
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true   # 使后续 git 命令使用 GITHUB_TOKEN
          fetch-depth: 0              # 深克隆，避免 stale info
      # ============================================================

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install requests
        run: pip install requests

      - name: Run script and save output
        run: python bestIp.py

      - name: Commit result
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add bestIp.txt log.txt
          git commit -m "Auto update bestIp.txt [skip ci]" || echo "No changes to commit"
          git push --force-with-lease
